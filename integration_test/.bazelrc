# Integration test Bazel configuration

# Use Bzlmod for dependency management
common --enable_bzlmod

# CI configuration - placeholder, actual repository_cache path set via --repository_cache flag
build:ci --announce_rc
test:ci --announce_rc
